#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @i: int to check
 * Return: absolute value of int
 */
int _abs(int i)
{
if (i > 0)
return (i);
else if (i < 0)
return (-i);
else
return (0);
}
